---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
- name: default
  run_list:
  - recipe[shared-spec-example]
  - recipe[export-node]

- name: nihilist
  attributes:
    nihilism: true
  run_list:
  - recipe[shared-spec-example]
  - recipe[export-node]
